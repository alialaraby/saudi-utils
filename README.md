# Saudi Utils

Saudi Utils is an ESM-first TypeScript package published as `saudi-utils`.

The package is currently private while its repository and release foundations are prepared.

## Development

Node.js 22 or newer is required.

```sh
npm ci
npm run check
```
